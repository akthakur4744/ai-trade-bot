---
description: Per-trade postmortem — reconstruct signal, exit, classify outcome
argument-hint: <position_id or symbol>
---

Execute the routine in @routines/postmortem-trade.md for: $ARGUMENTS

If no argument, ask for position ID or (symbol, entry_date). Append the one-line lesson to docs/trade_journal.md.
